# Example Figures

These figures illustrate the type of diagnostics produced by the workflow.

- `covariance_response_example.png`: observation-centered covariance-response diagnostic.
- `query_conditioned_weights_example.png`: query-conditioned historical weight diagnostic.

The figures are included as lightweight examples only. Restricted datasets,
model checkpoints, and internal HPC paths are not included.
